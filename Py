import datetime

# Add the Days Elapsed column with the Excel formula
buy_df['Days elapsed'] = buy_df['value date'].apply(lambda x: datetime.datetime.now().date() - x - datetime.timedelta(days=1) if pd.notnull(x) else '')

# Write the modified DataFrames to separate Excel files
buy_df.to_excel('buy_data.xlsx', index=False, sheet_name='Buy')
