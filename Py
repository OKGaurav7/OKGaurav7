# Define the columns to apply conditional formatting
    columns_to_format = ['Vd', 'Vd+1', 'Vd+2', 'Vd+3', 'Vd+4', 'Vd+5', 'Vd+6', 'Vd+7', 'Vd>7']

    # Loop through each column
    for column_name in columns_to_format:
        column_index = None
    for cell in ws[1]:
        if cell.value == column_name:
            column_index = cell.column
            break
    
    if column_index:
        # Set column width
        ws.column_dimensions[get_column_letter(column_index)].width = 6.5
        
        # Apply conditional formatting
        for row in ws.iter_rows(min_row=2):
            for cell in row:
                if cell.column == column_index and isinstance(cell.value, int) and cell.value > 0:
                    cell.fill = PatternFill(start_color="FFC7CE", end_color="FFC7CE", fill_type="solid")


this loop should check all the elements in the metioned columns and apply conditional formating, but it is only working for vd>7 columnn
