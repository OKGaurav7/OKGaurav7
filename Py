# Write data to the Excel file
for row_idx, row in enumerate(output_df.values, 2):
    for col_idx, value in enumerate(row, 1):
        ws.cell(row=row_idx, column=col_idx).value = str(value).encode('utf-8', 'ignore').decode('utf-8')
