import pandas as pd

# Assuming 'df' is your DataFrame
# Replace 'abc' and 'jkl' with your actual column names

for idx, row in df.iterrows():
    if pd.isnull(row['jkl']):  # Check if 'jkl' value is empty
        df.at[idx, 'jkl'] = row['abc']  # Replace empty 'jkl' value with 'abc' value in the same row
