AttributeError                            Traceback (most recent call last)
Cell In[60], line 84
     82     column = [cell.value for cell in column]
     83     max_length = max(len(str(cell)) for cell in column)
---> 84     ws.column_dimensions[chr(column[0].column + 64)].width = max_length + 2  # Adding some extra space
     86 # Save the modified Excel file
     87 wb.save(output_filename)

AttributeError: 'str' object has no attribute 'column'
