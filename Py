ValueError                                Traceback (most recent call last)
Cell In[159], line 163
    161 max_length = 0
    162 column_values = [cell.value for cell in column]
--> 163 max_length = max(len(str(cell)) for cell in column_values if cell is not None)
    164 column_letter = column[0].column_letter
    165 ws.column_dimensions[column_letter].width = max_length + 2  # Adding some extra space

ValueError: max() arg is an empty sequenc
